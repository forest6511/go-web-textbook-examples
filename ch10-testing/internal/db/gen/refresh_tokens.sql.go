// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: refresh_tokens.sql

package dbgen

import (
	"context"
	"time"

	"github.com/jackc/pgx/v5/pgtype"
)

const consumeRefreshToken = `-- name: ConsumeRefreshToken :one
UPDATE refresh_tokens
SET used_at = NOW()
WHERE id = $1 AND used_at IS NULL
RETURNING id
`

// used_at IS NULL の条件で原子的に used_at をセットする。
// 並行 /auth/refresh が 2 回叩かれた場合、1 回目だけが RETURNING で行を返し、
// 2 回目は pgx.ErrNoRows となる（= 再利用検知）。
func (q *Queries) ConsumeRefreshToken(ctx context.Context, id pgtype.UUID) (pgtype.UUID, error) {
	row := q.db.QueryRow(ctx, consumeRefreshToken, id)
	err := row.Scan(&id)
	return id, err
}

const deleteRefreshTokenByHash = `-- name: DeleteRefreshTokenByHash :exec
DELETE FROM refresh_tokens
WHERE token_hash = $1
`

func (q *Queries) DeleteRefreshTokenByHash(ctx context.Context, tokenHash []byte) error {
	_, err := q.db.Exec(ctx, deleteRefreshTokenByHash, tokenHash)
	return err
}

const findRefreshTokenByHash = `-- name: FindRefreshTokenByHash :one
SELECT id, user_id, token_hash, family_id, parent_id,
       used_at, revoked_at, expires_at, created_at
FROM refresh_tokens
WHERE token_hash = $1
`

func (q *Queries) FindRefreshTokenByHash(ctx context.Context, tokenHash []byte) (RefreshToken, error) {
	row := q.db.QueryRow(ctx, findRefreshTokenByHash, tokenHash)
	var i RefreshToken
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.TokenHash,
		&i.FamilyID,
		&i.ParentID,
		&i.UsedAt,
		&i.RevokedAt,
		&i.ExpiresAt,
		&i.CreatedAt,
	)
	return i, err
}

const insertRefreshToken = `-- name: InsertRefreshToken :one
INSERT INTO refresh_tokens (
    user_id, token_hash, family_id, parent_id, expires_at
)
VALUES ($1, $2, $3, $4, $5)
RETURNING id, user_id, token_hash, family_id, parent_id,
          used_at, revoked_at, expires_at, created_at
`

type InsertRefreshTokenParams struct {
	UserID    int64       `json:"user_id"`
	TokenHash []byte      `json:"token_hash"`
	FamilyID  pgtype.UUID `json:"family_id"`
	ParentID  pgtype.UUID `json:"parent_id"`
	ExpiresAt time.Time   `json:"expires_at"`
}

func (q *Queries) InsertRefreshToken(ctx context.Context, arg InsertRefreshTokenParams) (RefreshToken, error) {
	row := q.db.QueryRow(ctx, insertRefreshToken,
		arg.UserID,
		arg.TokenHash,
		arg.FamilyID,
		arg.ParentID,
		arg.ExpiresAt,
	)
	var i RefreshToken
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.TokenHash,
		&i.FamilyID,
		&i.ParentID,
		&i.UsedAt,
		&i.RevokedAt,
		&i.ExpiresAt,
		&i.CreatedAt,
	)
	return i, err
}

const insertRootRefreshToken = `-- name: InsertRootRefreshToken :one
INSERT INTO refresh_tokens (
    id, user_id, token_hash, family_id, expires_at
)
VALUES ($1, $2, $3, $1, $4)
RETURNING id, user_id, token_hash, family_id, parent_id,
          used_at, revoked_at, expires_at, created_at
`

type InsertRootRefreshTokenParams struct {
	ID        pgtype.UUID `json:"id"`
	UserID    int64       `json:"user_id"`
	TokenHash []byte      `json:"token_hash"`
	ExpiresAt time.Time   `json:"expires_at"`
}

func (q *Queries) InsertRootRefreshToken(ctx context.Context, arg InsertRootRefreshTokenParams) (RefreshToken, error) {
	row := q.db.QueryRow(ctx, insertRootRefreshToken,
		arg.ID,
		arg.UserID,
		arg.TokenHash,
		arg.ExpiresAt,
	)
	var i RefreshToken
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.TokenHash,
		&i.FamilyID,
		&i.ParentID,
		&i.UsedAt,
		&i.RevokedAt,
		&i.ExpiresAt,
		&i.CreatedAt,
	)
	return i, err
}

const revokeFamily = `-- name: RevokeFamily :exec
UPDATE refresh_tokens
SET revoked_at = NOW()
WHERE family_id = $1 AND revoked_at IS NULL
`

func (q *Queries) RevokeFamily(ctx context.Context, familyID pgtype.UUID) error {
	_, err := q.db.Exec(ctx, revokeFamily, familyID)
	return err
}
